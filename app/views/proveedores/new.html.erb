  <% form_for(@proveedor, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => { :f => f } %>

<br></br>
  <div align="center">
    <%= f.submit 'Crear' %>
  </div>
<% end %>

